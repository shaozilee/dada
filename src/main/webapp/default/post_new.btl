<!DOCTYPE html>
<html>
  <head>
    <title>Easy API Platform</title>
    <% include("_common.btl"){} %>
    <link rel="stylesheet" href="${ctxPath}/${theme}/css/index.css">
  </head>
  <body>
    <% include("_header.btl"){} %>
    <div>
      <form action="${ctxPath}/post/add.do" method="post" enctype="application/x-www-form-urlencoded">
        板块:
        <select name="sid">
          <% for(section in sections){%>
          <option value="${section.sid}">${section.name}</option>
          <% }%>
        </select>
        标题：<input type="text" name="subject"><br>
        内容：<textarea name="message" cols="30" rows="10"></textarea><br>
        <input type="submit" title="提交">
      </form>
    </div>
  </body>
</html>
